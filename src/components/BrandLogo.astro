---
const { class: className = '' } = Astro.props;
---

<div class={`inline-flex items-center gap-2 font-bold tracking-tight text-neutral-900 dark:text-neutral-100 ${className}`}>
  <span class="h-4 w-4 rounded-[5px] bg-emerald-500"></span>
  <span class="text-xl">Resolv</span>
</div>
